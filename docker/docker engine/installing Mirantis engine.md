you can find it on dockerhub, it has a free version for one month.

after installing it you'll see it in `docker version` command

if you're on centos you'll have to add a repo
then install it from yum.